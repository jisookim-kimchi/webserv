
server
{
    listen              8080;
    host                127.0.0.1;
    server_name         localhost;

    location /redirect
    {
        return          https://google.com;  # if no status code.
    }
}
